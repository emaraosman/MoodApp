<header>
  <h2> Login </h2>

  <%= form_for :user, url: session_path do |form| %>
    <%= form.text_field :username, placeholder: 'Username', required: true %>
    <%= form.password_field :password, placeholder: 'Password', required: true %>

    <%= submit_tag 'Submit' %>
  <% end %>
  <%= link_to 'Register', new_user_path %>

</header>
